"use client";
import {useEffect,useState} from "react"; import type {TocItem} from "@/lib/articles";
function Links({items,active}:{items:TocItem[];active:string}){return <div className="toc-links">{items.map(i=><a key={i.id} href={`#${i.id}`} aria-current={active===i.id?"location":undefined} className={`level-${i.level} ${active===i.id?"active":""}`}>{i.text}</a>)}</div>}
export function TableOfContents({items,mode}:{items:TocItem[];mode:"mobile"|"desktop"}){const [active,setActive]=useState("");useEffect(()=>{const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>a.boundingClientRect.top-b.boundingClientRect.top)[0];if(visible)setActive(visible.target.id)},{rootMargin:"-18% 0px -68% 0px"});items.forEach(i=>{const element=document.getElementById(i.id);if(element)observer.observe(element)});return()=>observer.disconnect()},[items]);if(!items.length)return null;return mode==="mobile"?<details className="mobile-toc"><summary>ON THIS PAGE</summary><Links items={items} active={active}/></details>:<aside className="toc" aria-label="Table of contents"><div className="toc-title">ON THIS PAGE</div><Links items={items} active={active}/></aside>}
